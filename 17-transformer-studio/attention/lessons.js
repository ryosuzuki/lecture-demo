export const lessons=[
{title:'One word. Several meanings.',name:'Context',time:110,scene:'context',formula:'same token embedding + different context → different update',idea:'An embedding starts without its context.',text:'“Mole” can name an animal, an amount of substance, or a skin lesion. A token lookup starts with the same vector. Attention provides a way for surrounding tokens to change it. These 3D directions are a conceptual illustration, not measured embeddings.',task:'Switch among the three phrases. Should the original mole vector change, or only the update?',answer:'The token embedding stays the same. Different context can produce different updates. Position information is separate from the token lookup.',controls:['context'],note:'The white vector is shared across all three examples. The coral endpoint is context-dependent.'},
{title:'Build a more specific tower.',name:'Meaning as movement',time:172,scene:'tower',formula:'e_tower → e_tower + ΔEiffel + Δminiature',idea:'Context can refine more than a word sense.',text:'Eiffel can make a generic tower more specific. Miniature can then change its implied scale. Farther through a model, vectors may encode much richer information than these simple attributes.',task:'Enable Eiffel, then miniature. Can a second update change a different property?',answer:'Yes. Updates are vectors, not replacement dictionary definitions. Here one changes identity and another reduces a size coordinate. The two controls illustrate the desired behavior, not a learned attention computation.',controls:['eiffel','miniature'],note:'Later attention blocks can transfer information that was already contextualized in earlier blocks.'},
{title:'Words become vectors — with position.',name:'Inputs & position',time:269,scene:'tokens',formula:'xᵢ = embedding(tokenᵢ) + positionᵢ',idea:'Use one sentence throughout the calculation.',text:'A fluffy blue creature roamed the verdant forest. Each token gets an input vector. We use 4 dimensions: two illustrative descriptor features, a noun feature, and a position feature. Only the first three are spatial coordinates; position is shown numerically.',task:'Select creature and forest. Turn position strength to zero. Can this toy distinguish their input vectors now?',answer:'No. We deliberately gave the two nouns the same content features. With position disabled their inputs are identical. Real models have different token embeddings and richer position encodings.',controls:['tokens','pos'],note:'This lesson uses row vectors. The video often uses column vectors. The matrix displays are transposed conventions, not different algorithms.'},
{title:'One input, three learned projections.',name:'Query, Key, Value',time:374,scene:'projection',formula:'Q = XWQ     K = XWK     V = XWV',idea:'Routing and content are different jobs.',text:'A Query describes what a receiving token seeks. A Key describes what a source can match. A Value supplies the content to transfer. The same projection matrices are applied to every token; they are model parameters, while Q, K, and V depend on the input.',task:'Change the selected token. Which things change: the projection matrices, the projected vectors, or both?',answer:'Only the projected vectors change. The matrices are shared across tokens. Real weights are learned; the small weights here are hand-designed to illustrate the adjective-to-noun story.',controls:['tokens','head'],note:'A semantic interpretation of an actual head is not guaranteed. “Looking for adjectives” is an explanatory example.'},
{title:'Alignment becomes a dot product.',name:'Drag a Query',time:487,scene:'dot',formula:'scoreᵢⱼ = qᵢ · kⱼ / √dₖ',idea:'Direction AND length determine a match.',text:'Drag the gold Query tip in the plane, or use the two sliders. Teal Keys remain fixed. This directly intervenes on one Query after projection; it does not retrain the model. The exact dot products below update as you move.',task:'Aim Q toward fluffy, then blue, then the opposite direction. Can a raw score be negative?',answer:'Yes. Opposite directions can produce negative dot products. Scores are not probabilities. Increasing length can also increase a dot product without changing direction.',controls:['qx','qy','tokens'],note:'Only the selected receiving Query is changed. Editing a Value later affects every receiver that reads that source in the selected head.'},
{title:'Every Query meets every Key.',name:'Attention pattern',time:511,scene:'pattern',formula:'S = QKᵀ / √dₖ     S has n × n entries',idea:'An entire sentence is processed in parallel.',text:'Each row is a receiving Query; each column is a source Key. Click a cell in the table or a 3D bar to inspect that pair. A larger positive bar means a stronger raw match. Negative scores extend below the plane.',task:'Compare the creature and forest rows. Why does a high raw score not necessarily mean information can flow?',answer:'Raw compatibility is only the first step. A causal mask can forbid a future source even if its score is high, and softmax determines its relative share.',controls:['tokens','head'],note:'All pairs use the same matrix product. An attention weight is not, by itself, a complete causal explanation of a model output.'},
{title:'Scores become a distribution.',name:'Softmax',time:555,scene:'softmax',formula:'aⱼ = exp(sⱼ / τ − m) / Σₖ exp(sₖ / τ − m)',idea:'Positive shares, with a total of one.',text:'Exponentiate the scores and divide by their sum. Subtracting the largest logit m keeps the computation numerically stable without changing the answer. Our optional temperature control changes sharpness; it is an experiment, not an extra required GPT parameter.',task:'Lower temperature. Do all scores increase, or does the distribution become more concentrated?',answer:'The logits are rescaled and the largest ones gain relative probability. The normalized weights still sum to one. This is attention temperature, not the sampling temperature used to select a generated token.',controls:['temp','tokens','head'],note:'This chapter temporarily shows unmasked softmax so you can isolate normalization. The next chapter adds causal masking.'},
{title:'Do not leak the answer.',name:'Causal masking',time:650,scene:'mask',formula:'future logits = −∞ → exp(−∞) = 0',idea:'Predict all next tokens without looking ahead.',text:'Training can predict the next token at every position at once. A future token would reveal an answer, so its logit is set to negative infinity BEFORE softmax. The remaining weights are renormalized. The diagonal is allowed.',task:'Select creature. Toggle the mask off. Can it now read verdant and forest? Restore the mask and check the row sum.',answer:'With the mask off, future tokens can receive weight. With it on, every future weight is exactly zero and the row still sums to one. Zeroing weights after softmax without renormalizing would be wrong.',controls:['tokens','mask','temp'],note:'Self-attention in a causal language model uses the mask. Bidirectional attention in other architectures need not.'},
{title:'Values carry the actual information.',name:'Weighted Values',time:790,scene:'values',formula:'Δeᵢ = Σⱼ aᵢⱼ (vⱼ Wup)     e′ᵢ = eᵢ + Δeᵢ',idea:'Keep the route. Change the payload.',text:'The colored arrows are weighted Value contributions, placed head-to-tail from the white original vector. The coral arrow ends at the updated representation. Scrub the scene to assemble the sum. The 4th coordinate is preserved but not drawn as a spatial axis.',task:'Set fluffy Value gain to zero, then negative. Do the attention weights change?',answer:'No. Q and K determine the attention pattern; this intervention changes only the fluffy Value. The weighted update changes, but A does not. It changes that source Value for all receivers in the selected head.',controls:['gain','tokens','head'],note:'Changing an input embedding would generally change its Q, K, and V together. Here we isolate V on purpose.'},
{title:'A large update through a narrow channel.',name:'Low-rank Values',time:968,scene:'lowrank',formula:'x (1×4) → WV (4×2) → v (1×2) → Wup (2×4)',idea:'Down-project, then map back to model space.',text:'Instead of one large square value map per head, factor it into two narrower matrices. Their product has rank at most the head dimension. “Up” does not invert “down”: it creates an update, not a reconstruction of the entire input.',task:'Select creature, then fluffy. Is the lifted Value equal to the original input?',answer:'Usually not. Here creature’s content does not create a Value update, while fluffy does. The position and noun coordinates are discarded by this Value map. Low rank limits the subspace of possible updates.',controls:['tokens','head'],note:'In standard notation WV is the down projection. Each head’s up projection is a block of the shared output matrix WO.'},
{title:'Queries can come from a different stream.',name:'Cross-attention',time:1100,scene:'cross',formula:'Q = target WQ     K = source WK     V = source WV',idea:'Same operation, different sources.',text:'In translation, a partial target sentence can query a source sentence. This small two-query / three-source example illustrates the rectangular attention matrix. Keys AND Values come from the source, Queries from the target. The illustrated weights are not learned translations.',task:'Move target Query 1 with the sliders. Does Query 2 change too?',answer:'No. Each query has its own normalized distribution over the source. The matrix is 2×3, not necessarily square. An encoder–decoder source stream usually needs no causal future mask here, though other masks can still apply.',controls:['qx','qy'],note:'Source labels: le / chat / dort. Target queries: the / cat. The numeric maps are illustrative, not linguistic alignment measurements.'},
{title:'Several heads propose different updates.',name:'Multi-head & WO',time:1160,scene:'multi',formula:'Σₕ OₕWup,ₕ = Concat(O₁, O₂, O₃) WO',idea:'Parallel heads; one combined residual update.',text:'Each head has its own Q, K, and Value maps. The example heads favor descriptors differently. Enable and disable them to inspect their contributions. The output matrix combines their concatenated low-dimensional results before the residual addition.',task:'Disable one head. Does the result equal the sum of the remaining heads? Compare sum and concat calculations below.',answer:'Yes, exactly up to floating-point rounding. Stacking the per-head up matrices into WO makes concatenation-plus-projection algebraically identical to summing each projected head output.',controls:['heads','tokens'],note:'Real heads need not have tidy names like “adjective head.” Independent projections allow many learned patterns, including less interpretable ones.'},
{title:'Count the cost of attention.',name:'Scale & parameters',time:1323,scene:'scale',formula:'pairs = n²     attention weights ≈ 4 × d × dₖ × heads × layers',idea:'Context cost and parameter count are different.',text:'A dense attention pattern has n² entries per head. Doubling the context quadruples the pair count, but does not add learned projection parameters. GPT-3’s largest configuration used d=12,288, dₖ=128, 96 heads, and 96 layers.',task:'Double context length. Then double layers. Which changes the pair count, and which changes learned parameters?',answer:'Context length changes the number of pairs. Layer count changes the number of distinct parameter sets. The count here excludes biases, normalization, embeddings, and MLPs. Memory-efficient kernels need not materialize the entire n² matrix.',controls:['contextSize','layers'],note:'Per head: 6,291,456. Per block: 603,979,776. Across 96 blocks: 57,982,058,496 attention projection parameters.'},
{title:'Contextualize, then contextualize again.',name:'Layers & the big picture',time:1399,scene:'stack',formula:'[attention → MLP] × many blocks → last vector → next-token logits',idea:'Later layers receive already richer inputs.',text:'Attention mixes information across positions; MLPs transform each position’s representation. Transformers repeat both, with residual connections and normalization. The final position is read out to predict the next token. This scene is architectural, not a numerical GPT simulation.',task:'Move through the stack. Why can the final vector represent more than the last word alone?',answer:'Repeated attention can bring relevant context into that position, including information already combined elsewhere. The last vector can support next-token prediction. Parallel matrix operations make the architecture efficient to train at scale.',controls:['depth'],note:'Attention is only part of a Transformer. The original video defers MLP and training details to another chapter; this lesson marks that boundary too.'}
];
